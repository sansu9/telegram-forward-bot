# 真实配置——从 config.example.py 复制而来，包含你的账号信息，绝不能提交
config.py

# Telethon 会话文件——等同于账号登录凭证，泄露 = 账号被接管
*.session
*.session-journal

# 去重功能生成的运行时数据，每台机器上都不一样，不用提交
seen_messages.json

# Python
__pycache__/
*.pyc
venv/
.venv/
env/

# 系统文件
.DS_Store
